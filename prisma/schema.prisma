generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id                     String                 @id @default(cuid())
  name                   String
  username               String?                @unique
  email                  String                 @unique
  emailVerified          DateTime?
  isEmailVerified        Boolean                @default(false)
  image                  String?                @default("/globe.svg")
  password               String?
  role                   UserRole               @default(CLIENT)
  isTwoFactorEnabled     Boolean                @default(false)
  twoFactorPreferredMethod String               @default("TOTP")
  twoFactorEmailFallbackEnabled Boolean         @default(true)
  createdAt              DateTime               @default(now())
  updatedAt              DateTime               @updatedAt
  apellido               String?
  ciudadDeResidencia     String?
  estadoDeResidencia     String?
  onboardingStatus       OnboardingStatus       @default(PENDIENTE)
  telefono               String?                @unique
  expediente             String?
  materiasActuales       String?                @db.Text
  semestreActual         String?
  semestreActualManual   Boolean                @default(false)
  caletaTutorialCompleted Boolean               @default(false)
  universidadId          String?
  carreraId              String?
  accounts               Account[]
  authSessions           AuthSession[]
  authAccounts           AuthAccount[]
  authTwoFactors         TwoFactor[]
  passkeys               Passkey[]
  blogPosts              BlogPost[]
  mediaAssets            MediaAsset[]
  StripeCustomer         StripeCustomer?
  twoFactorConfirmation  TwoFactorConfirmation?
  universidad            Universidad?           @relation(fields: [universidadId], references: [id])
  carrera                Carrera?               @relation(fields: [carreraId], references: [id])
  UserSubscription       UserSubscription[]
  notifications          Notification[]
  materiasEstudiante     MateriaEstudiante[]
  recursos                Recurso[]
  calificaciones         CalificacionRecurso[]
  comentarios            ComentarioRecurso[]
  vistas                  VistaRecurso[]
  descargas              DescargaRecurso[]
  likes                  LikeRecurso[]
  metas                  MetaAcademica[]
  favoritos              Favorito[]
  manualPayments         ManualPayment[]
  manualPaymentsReviewed ManualPayment[] @relation("ManualPaymentReviewedBy")
  cursos                 Curso[]
  paymentRecords         PaymentRecord[]
  pensumVersions         PensumVersion[]      @relation("PensumVersionCreatedBy")
  tesisDocumentos      TesisDocumento[]
  following              UserFollow[]         @relation("FollowsInitiated")
  followers              UserFollow[]         @relation("FollowsReceived")
  profileBio             String?              @db.Text
  profileBannerUrl       String?
  profileGalleryUrls     Json?                // array de URLs (strings), p. ej. ["https://..."]
  historias                Historia[]
  historiaLikes            HistoriaLike[]
  calendarEvents           CalendarEvent[]
  referralCode             String?                @unique
  referredByUserId         String?
  referredBy               User?                  @relation("UserReferrals", fields: [referredByUserId], references: [id], onDelete: SetNull)
  referredUsers            User[]                 @relation("UserReferrals")
  referralBoosts           ReferralBoost[]
  walletBalanceCents      Int                    @default(0)
  /** 0–30: descuento en consumo de IA (p. ej. 30 si usó código del admin). */
  iaConsumptionDiscountPercent Int                @default(0)
  /** Modelo IA elegido para chat (ID Gateway o OpenAI); null = default del servidor. */
  iaModelChat             String?
  /** Modelo para resumir, fichas, cuestionario (rol “heavy”). */
  iaModelHeavy            String?
  /** Modelo para interpretar cronograma desde texto. */
  iaModelCronograma       String?
  /** `auto` = elige modelo según la petición y cuenta; `manual` = usa el `iaModel*` de ese rol (o default). */
  iaLlmModeChat           String                 @default("manual")
  iaLlmModeHeavy          String                 @default("manual")
  iaLlmModeCronograma     String                 @default("manual")
  walletLedger             WalletLedger[]
  caletaTasks              CaletaTask[]
  /** JSON: [{ status, label, colorKey }] — columnas personalizadas del tablero de tareas. */
  caletaTaskBoardColumns   Json?
  iaFreeTierDaily          IaFreeTierDaily[]
  aprendePic18Progress     AprendePic18Progress?
  aprendeCppPooProgress    AprendeCppPooProgress?

  @@index([role])
  @@index([universidadId])
  @@index([referredByUserId])
}

enum CaletaTaskPriority {
  BAJA
  MEDIA
  ALTA
}

model CaletaTask {
  id          String             @id @default(cuid())
  userId      String
  user        User               @relation(fields: [userId], references: [id], onDelete: Cascade)
  title       String
  description String?            @db.Text
  status      String             @default("PENDIENTE")
  priority    CaletaTaskPriority @default(MEDIA)
  dueAt       DateTime?
  icon        String?
  createdAt   DateTime           @default(now())
  updatedAt   DateTime           @updatedAt

  @@index([userId, status])
  @@index([userId, dueAt])
  @@index([userId, createdAt])
}

enum ReferralBoostKind {
  REFEREE_WELCOME
  REFERRER_REWARD
}

enum ReferralBoostStatus {
  PENDING
  ACTIVE
}

/** Día(s) de IA sin límites de prueba: bienvenida por referido o recompensa pendiente al referidor. */
model ReferralBoost {
  id                String              @id @default(cuid())
  userId            String
  user              User                @relation(fields: [userId], references: [id], onDelete: Cascade)
  kind              ReferralBoostKind
  status            ReferralBoostStatus @default(PENDING)
  startsAt          DateTime?
  endsAt            DateTime?
  /** Usuario que se registró con tu código (solo recompensa al referidor). */
  triggeredByUserId String?
  createdAt         DateTime            @default(now())

  @@index([userId, status])
  @@index([kind, status])
}

model WalletLedger {
  id                 String   @id @default(cuid())
  userId             String
  user               User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  /** Negativo = cargo por IA; positivo = recarga/crédito admin. */
  deltaCents         Int
  balanceAfterCents  Int
  reason             String   @db.VarChar(64)
  meta               Json?
  createdAt          DateTime @default(now())

  @@index([userId, createdAt])
}

model CalendarEvent {
  id          String   @id @default(cuid())
  userId      String
  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  title       String
  activityType String?
  description String?  @db.Text
  location    String?

  startAt     DateTime
  endAt       DateTime
  allDay      Boolean  @default(false)
  reminderMinutes Int? @default(30)
  reminderSentAt DateTime?

  color       String?  // hex o token UI (p.ej. "#AABBCC")
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([userId, startAt])
  @@index([userId, endAt])
  @@index([reminderSentAt, startAt])
}

model UserFollow {
  id          String   @id @default(cuid())
  followerId  String
  followingId String
  createdAt   DateTime @default(now())
  follower    User     @relation("FollowsInitiated", fields: [followerId], references: [id], onDelete: Cascade)
  following   User     @relation("FollowsReceived", fields: [followingId], references: [id], onDelete: Cascade)

  @@unique([followerId, followingId])
  @@index([followerId])
  @@index([followingId])
}

/** Historia efímera (foto o video); el archivo vive en Bunny bajo prefijo `historia/`. */
model Historia {
  id        String            @id @default(cuid())
  autorId   String
  autor     User              @relation(fields: [autorId], references: [id], onDelete: Cascade)
  mediaUrl  String            @db.Text
  mediaType HistoriaMediaType
  expiresAt DateTime
  createdAt DateTime          @default(now())
  likes     HistoriaLike[]

  @@index([expiresAt])
  @@index([autorId, expiresAt])
}

model HistoriaLike {
  id         String   @id @default(cuid())
  historiaId String
  historia   Historia @relation(fields: [historiaId], references: [id], onDelete: Cascade)
  userId     String
  user       User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  createdAt  DateTime @default(now())

  @@unique([historiaId, userId])
  @@index([historiaId])
  @@index([userId])
}

enum HistoriaMediaType {
  IMAGE
  VIDEO
}

model AuthSession {
  id        String   @id
  expiresAt DateTime
  token     String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  ipAddress String?  @db.Text
  userAgent String?  @db.Text
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([token])
  @@index([userId])
  @@map("ba_session")
}

model AuthAccount {
  id                    String    @id
  accountId             String    @db.Text
  providerId            String    @db.Text
  userId                String
  user                  User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  accessToken           String?   @db.Text
  refreshToken          String?   @db.Text
  idToken               String?   @db.Text
  accessTokenExpiresAt  DateTime?
  refreshTokenExpiresAt DateTime?
  scope                 String?   @db.Text
  password              String?   @db.Text
  createdAt             DateTime  @default(now())
  updatedAt             DateTime  @updatedAt

  @@index([userId])
  @@map("ba_account")
}

model AuthVerification {
  id         String   @id
  identifier String   @db.Text
  value      String   @db.Text
  expiresAt  DateTime
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  @@index([identifier])
  @@map("ba_verification")
}

model TwoFactor {
  id          String @id
  secret      String @db.Text
  backupCodes String @db.Text
  userId      String
  user        User   @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([secret])
  @@index([userId])
  @@map("ba_twoFactor")
}

model Passkey {
  id           String   @id
  name         String?  @db.Text
  publicKey    String   @db.Text
  userId       String
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  credentialID String   @db.Text
  counter      Int
  deviceType   String   @db.Text
  backedUp     Boolean
  transports   String?  @db.Text
  createdAt    DateTime?
  aaguid       String?  @db.Text

  @@index([userId])
  @@index([credentialID])
  @@map("passkey")
}

model Account {
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String?
  access_token      String?
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String?
  session_state     String?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
  user              User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@id([provider, providerAccountId])
  @@index([userId], map: "Account_userId_idx")
}

model VerificationToken {
  id      String   @id @default(cuid())
  email   String
  token   String   @unique
  expires DateTime

  @@unique([email, token])
}

model PasswordResetToken {
  id      String   @id @default(cuid())
  email   String
  token   String   @unique
  expires DateTime

  @@unique([email, token])
}

model TwoFactorToken {
  id      String   @id @default(cuid())
  email   String
  token   String   @unique
  expires DateTime

  @@unique([email, token])
}

model TwoFactorConfirmation {
  id     String @id @default(cuid())
  userId String @unique
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model StripeCustomer {
  id               String   @id @default(uuid())
  userId           String   @unique
  stripeCustomerId String   @unique
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
  user             User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model UserSubscription {
  id        String   @id @default(uuid())
  userId    String
  subscriptionTypeId String?
  stripeCustomerId String?
  stripeSubscriptionId String?
  stripePriceId String?
  stripeCurrentPeriodEnd DateTime?
  /** Tokens (prompt+completion) consumidos del cupo incluido en el periodo actual de facturación. */
  iaIncludedTokensUsed Int @default(0)
  /** Copia de stripeCurrentPeriodEnd cuando se reinició el contador de tokens incluidos. */
  iaMeterPeriodEnd DateTime?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  subscriptionType SubscriptionType? @relation(fields: [subscriptionTypeId], references: [id], onDelete: SetNull)

  @@index([userId])
  @@index([subscriptionTypeId])
}

model SubscriptionType {
  id          String   @id @default(uuid())
  name        String
  description String?  @db.Text
  price       Int
  period      String
  /** stripe_recurring | wallet_consumption (CALETA BASICS: misma billetera, sin Stripe). */
  billingKind String   @default("stripe_recurring")
  /** Recarga mínima en centavos USD para planes solo billetera (p. ej. 100 = $1). */
  minWalletTopUpCents Int @default(0)
  /** Cupo mensual de tokens (prompt+completion) incluidos con la suscripción; null = sin tope (legacy). */
  includedIaTokensPerPeriod Int?
  /** Tras agotar el cupo: wallet = cobrar billetera; block = no más IA de consumo hasta el próximo ciclo. */
  iaTokenOverflowPolicy String @default("wallet")
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  subscriptions UserSubscription[]
  manualPayments ManualPayment[]
  paymentRecords PaymentRecord[]
  iaAccessRules SubscriptionTypeIaAccess[]
}

/// Catálogo de funciones IA (referencia a precios listos tipo Vercel AI Gateway).
model IaFeatureCatalog {
  id                  String   @id @default(cuid())
  featureKey          String   @unique
  displayNameEs       String
  descriptionEs       String?  @db.Text
  vercelPricingUrl    String   @db.Text
  listInputUsdPer1M   Float?
  listOutputUsdPer1M  Float?
  referenceModelIds   String?  @db.Text
  /** Gratis solo dentro del cupo de prueba (sin suscripción ni saldo en billetera). */
  freeInTrial         Boolean  @default(false)
  /** Tras la prueba o con saldo: se cobra por consumo (tokens + margen). */
  meteredAfterTrial   Boolean  @default(true)
  sortOrder           Int      @default(0)

  planAccess SubscriptionTypeIaAccess[]
}

/// Qué significa cada función IA dentro de un plan (para la página de suscripción).
model SubscriptionTypeIaAccess {
  id                   String   @id @default(cuid())
  subscriptionTypeId String
  iaFeatureCatalogId  String
  /** consumption | included | blocked */
  accessKind           String   @default("consumption")
  notesEs              String?  @db.Text

  subscriptionType SubscriptionType @relation(fields: [subscriptionTypeId], references: [id], onDelete: Cascade)
  catalog          IaFeatureCatalog @relation(fields: [iaFeatureCatalogId], references: [id], onDelete: Cascade)

  @@unique([subscriptionTypeId, iaFeatureCatalogId])
  @@index([subscriptionTypeId])
}



model Universidad {
  id        String    @id @default(uuid())
  nombre    String    @unique
  siglas    String    @unique
  direccion String
  telefono  String
  email     String
  website   String?
  estado    String
  ciudad    String
  tipo      String    @default("PÚBLICA")
  ranking   Int?
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  password  String?
  isActive  Boolean   @default(true)
  carreras  Carrera[]
  users     User[]
  pensumVersions PensumVersion[]
  recursos  Recurso[]
}

model Carrera {
  id            String      @id @default(uuid())
  nombre        String
  codigo        String      @unique
  descripcion   String?     @db.Text
  duracion      Int
  creditos      Int
  universidadId String
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @updatedAt
  isActive      Boolean     @default(true)
  universidad   Universidad @relation(fields: [universidadId], references: [id], onDelete: Cascade)
  materias      Materia[]
  usuarios      User[]
  pensumVersions PensumVersion[]

  @@index([universidadId])
}

enum PensumVersionStatus {
  DRAFT
  PUBLISHED
  ARCHIVED
}

model PensumVersion {
  id             String             @id @default(cuid())
  universidadId  String
  carreraId      String
  versionNumber  Int
  name           String
  notes          String?            @db.Text
  status         PensumVersionStatus @default(DRAFT)
  snapshot       Json
  createdById    String
  createdAt      DateTime           @default(now())
  publishedAt    DateTime?

  universidad    Universidad        @relation(fields: [universidadId], references: [id], onDelete: Cascade)
  carrera        Carrera            @relation(fields: [carreraId], references: [id], onDelete: Cascade)
  createdBy      User               @relation("PensumVersionCreatedBy", fields: [createdById], references: [id], onDelete: Cascade)

  @@index([universidadId])
  @@index([carreraId])
  @@index([status])
  @@unique([carreraId, versionNumber])
}

model Materia {
  id                String                 @id @default(uuid())
  nombre            String
  codigo            String                 @unique
  descripcion       String?                @db.Text
  creditos          Int
  semestre          Semestre
  horasTeoria       Int
  horasPractica     Int
  carreraId         String
  createdAt         DateTime               @default(now())
  updatedAt         DateTime               @updatedAt
  isActive          Boolean                @default(true)
  carrera           Carrera                @relation(fields: [carreraId], references: [id], onDelete: Cascade)
  prerrequisitos    MateriaPrerrequisito[] @relation("MateriaPrerrequisito")
  esPrerrequisitoDe MateriaPrerrequisito[] @relation("MateriaQueRequiere")
  materiasEstudiante MateriaEstudiante[]
  recursos           Recurso[]

  @@index([carreraId])
}

model MateriaPrerrequisito {
  id                String            @id @default(uuid())
  materiaId         String
  prerrequisitoId   String
  tipoPrerrequisito TipoPrerrequisito @default(OBLIGATORIO)
  createdAt         DateTime          @default(now())
  materia           Materia           @relation("MateriaPrerrequisito", fields: [materiaId], references: [id], onDelete: Cascade)
  prerrequisito     Materia           @relation("MateriaQueRequiere", fields: [prerrequisitoId], references: [id], onDelete: Cascade)

  @@unique([materiaId, prerrequisitoId])
  @@index([materiaId])
  @@index([prerrequisitoId])
}

model MateriaEstudiante {
  id                String              @id @default(uuid())
  userId            String
  materiaId         String
  estado            EstadoMateria       @default(NO_CURSADA)
  nota              Float?
  semestreCursado   String?
  fechaInicio       DateTime?
  fechaFin          DateTime?
  observaciones     String?             @db.Text
  createdAt         DateTime            @default(now())
  updatedAt         DateTime            @updatedAt
  user              User                @relation(fields: [userId], references: [id], onDelete: Cascade)
  materia           Materia             @relation(fields: [materiaId], references: [id], onDelete: Cascade)

  @@unique([userId, materiaId])
  @@index([userId])
  @@index([materiaId])
}

enum BlogPostStatus {
  DRAFT
  PUBLISHED
}

enum BlogCategory {
  NOVEDADES
  CONSEJOS_ESTUDIO
  VIDA_UNIVERSITARIA
  RECURSOS_ACADEMICOS
  CARRERA
  TECNOLOGIA
  TUTORIALES
  COMUNIDAD
}

model BlogPost {
  id              String         @id @default(cuid())
  slug            String         @unique
  title           String
  excerpt         String         @db.Text
  content         String         @db.Text
  metaTitle       String?
  metaDescription String?        @db.Text
  coverImage      String?
  category        BlogCategory
  status          BlogPostStatus @default(DRAFT)
  publishedAt     DateTime?
  authorId        String
  createdAt       DateTime       @default(now())
  updatedAt       DateTime       @updatedAt
  author          User           @relation(fields: [authorId], references: [id], onDelete: Cascade)

  @@index([status, publishedAt])
  @@index([category, status])
  @@index([authorId], map: "BlogPost_authorId_idx")
}

/// Imagen almacenada en Bunny.net con metadatos para la galería de medios del blog.
model MediaAsset {
  id           String   @id @default(cuid())
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  path         String   @unique
  url          String
  folder       String
  filename     String
  mimeType     String?
  sizeBytes    Int?
  title        String?  @db.VarChar(191)
  altText      String?  @db.VarChar(191)
  description  String?  @db.Text
  uploadedById String?
  uploadedBy   User?    @relation(fields: [uploadedById], references: [id], onDelete: SetNull)

  @@index([folder])
  @@index([createdAt])
}

// Cursos y tutoriales subidos por admin para estudiantes
model Curso {
  id          String   @id @default(cuid())
  titulo      String
  slug        String?  @unique
  tipo        String   @default("video")
  descripcion String   @db.Text
  contenido   String   @db.Text
  urlVideo    String?
  externalUrl String?
  imagenUrl   String?
  tema        String?  // Tema/categoría (ej. Programación, Matemáticas)
  orden       Int      @default(0)
  autorId    String
  autor       User     @relation(fields: [autorId], references: [id], onDelete: Cascade)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([autorId])
  @@index([tema])
}

model Notification {
  id        String   @id @default(uuid())
  user      User     @relation(fields: [userId], references: [id])
  userId    String
  message   String
  read      Boolean  @default(false)
  createdAt DateTime @default(now())
}

model Recurso {
  id               String   @id @default(cuid())
  titulo           String
  descripcion      String
  tipo             TipoRecurso
  contenido        String   // Contenido del recurso (texto, enlaces, etc.)
  archivoUrl       String?  // URL del archivo si se sube uno
  archivoSizeBytes Int?     // Tamaño en bytes del archivo subido (para estadísticas admin)
  materiaId        String?
  materia     Materia?  @relation(fields: [materiaId], references: [id], onDelete: Cascade)
  universidadId    String?
  universidad      Universidad? @relation(fields: [universidadId], references: [id], onDelete: SetNull)
  autorId     String
  autor       User     @relation(fields: [autorId], references: [id], onDelete: Cascade)
  calificacion Float   @default(0) // Promedio de calificaciones
  numCalificaciones Int @default(0) // Número de calificaciones
  numVistas   Int      @default(0) // Número de veces visto
  numDescargas Int     @default(0) // Número de descargas
  esPublico   Boolean  @default(true)
  esAnonimo   Boolean  @default(false)
  tags        String   // Tags para categorización (separados por comas)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  // Relaciones
  calificaciones CalificacionRecurso[]
  comentarios    ComentarioRecurso[]
  vistas         VistaRecurso[]
  descargas      DescargaRecurso[]
  favoritos      Favorito[]
  likes          LikeRecurso[]

  @@index([materiaId])
  @@index([universidadId])
  @@index([autorId])
  @@index([tipo])
  @@index([esPublico])
  @@index([esAnonimo])
}

// Contenidos de consulta en app (sin descarga); acceso planes $3+ y $7+
model BibliotecaObra {
  id          String   @id @default(cuid())
  titulo      String
  slug        String   @unique
  descripcion String?  @db.Text
  cuerpo      String   @db.Text
  orden       Int      @default(0)
  isPublished Boolean  @default(false)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([isPublished])
  @@index([orden])
}

// Documentos privados del estudiante (tesis/monografías) editados en el editor tipo Overleaf.
model TesisDocumento {
  id               String   @id @default(cuid())
  titulo           String
  cuerpo           String   @db.Text
  headerTemplate   String   @db.Text @default("{{title}}")
  footerTemplate   String   @db.Text @default("Página {{page}} de {{pages}}")
  paperSize        String   @default("a4") // a4 | letter | tabloid
  paperMode        String   @default("light") // light | dim (solo visual)
  zoom             Int      @default(100)
  fontScale        Float    @default(0.92)
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  ownerId          String
  owner            User     @relation(fields: [ownerId], references: [id], onDelete: Cascade)

  @@index([ownerId])
  @@index([updatedAt])
}

model CalificacionRecurso {
  id          String   @id @default(cuid())
  recursoId   String
  recurso     Recurso  @relation(fields: [recursoId], references: [id], onDelete: Cascade)
  usuarioId   String
  usuario     User     @relation(fields: [usuarioId], references: [id], onDelete: Cascade)
  calificacion Int     // 1-5 estrellas
  comentario  String?  // Comentario opcional
  createdAt   DateTime @default(now())

  @@unique([recursoId, usuarioId]) // Un usuario solo puede calificar una vez
  @@index([recursoId])
  @@index([usuarioId])
}

model ComentarioRecurso {
  id          String   @id @default(cuid())
  recursoId   String
  recurso     Recurso  @relation(fields: [recursoId], references: [id], onDelete: Cascade)
  autorId     String
  autor       User     @relation(fields: [autorId], references: [id], onDelete: Cascade)
  contenido   String
  esRespuesta Boolean  @default(false) // Si es respuesta a otro comentario
  comentarioPadreId String? // ID del comentario padre si es respuesta
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([recursoId])
  @@index([autorId])
}

model VistaRecurso {
  id          String   @id @default(cuid())
  recursoId   String
  recurso     Recurso  @relation(fields: [recursoId], references: [id], onDelete: Cascade)
  usuarioId   String?
  usuario     User?    @relation(fields: [usuarioId], references: [id], onDelete: SetNull)
  ipAddress   String?  // Para tracking de vistas anónimas
  userAgent   String?  // Información del navegador
  createdAt   DateTime @default(now())

  @@index([recursoId])
  @@index([usuarioId])
}

model DescargaRecurso {
  id          String   @id @default(cuid())
  recursoId   String
  recurso     Recurso  @relation(fields: [recursoId], references: [id], onDelete: Cascade)
  usuarioId   String
  usuario     User     @relation(fields: [usuarioId], references: [id], onDelete: Cascade)
  createdAt   DateTime @default(now())

  @@index([recursoId])
  @@index([usuarioId])
}

model MetaAcademica {
  id          String   @id @default(cuid())
  usuarioId   String
  usuario     User     @relation(fields: [usuarioId], references: [id], onDelete: Cascade)
  titulo      String
  descripcion String?
  tipo        TipoMeta
  valorObjetivo Float  // Valor objetivo (promedio, materias, etc.)
  valorActual  Float   @default(0)
  fechaLimite DateTime?
  completada  Boolean  @default(false)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([usuarioId])
  @@index([tipo])
  @@index([completada])
}

enum UserRole {
  CLIENT
  ADMIN
}

enum ManualPaymentStatus {
  PENDING
  APPROVED
  REJECTED
}

model ManualPayment {
  id                 String              @id @default(uuid())
  userId             String
  subscriptionTypeId String
  amountBs           Int
  reference          String
  proofUrl           String?
  status             ManualPaymentStatus @default(PENDING)
  reviewedAt         DateTime?
  reviewedById       String?
  createdAt          DateTime            @default(now())
  updatedAt          DateTime            @updatedAt

  user               User                @relation(fields: [userId], references: [id], onDelete: Cascade)
  subscriptionType   SubscriptionType    @relation(fields: [subscriptionTypeId], references: [id], onDelete: Cascade)
  reviewedBy         User?               @relation("ManualPaymentReviewedBy", fields: [reviewedById], references: [id], onDelete: SetNull)
  paymentRecord      PaymentRecord?

  @@index([userId])
  @@index([subscriptionTypeId])
  @@index([status])
  @@index([reviewedById])
}

enum PaymentSource {
  MOBILE_BS
  CARD
}

model PaymentRecord {
  id                    String              @id @default(cuid())
  userId                String
  subscriptionTypeId    String?
  source                PaymentSource
  status                ManualPaymentStatus @default(PENDING)
  amountBs              Int?
  amountUsdCents        Int?
  reference             String?
  operationCode         String?
  rejectionReason       String?             @db.Text
  periodStart           DateTime?
  periodEnd             DateTime?
  paidAt                DateTime?
  manualPaymentId       String?             @unique
  stripeInvoiceId       String?             @unique
  stripePaymentIntentId String?
  createdAt             DateTime            @default(now())
  updatedAt             DateTime            @updatedAt

  user                  User                @relation(fields: [userId], references: [id], onDelete: Cascade)
  subscriptionType      SubscriptionType?   @relation(fields: [subscriptionTypeId], references: [id], onDelete: SetNull)
  manualPayment         ManualPayment?      @relation(fields: [manualPaymentId], references: [id], onDelete: SetNull)

  @@index([userId])
  @@index([subscriptionTypeId])
  @@index([status])
  @@index([source])
  @@index([periodEnd])
}

enum Estado {
  PENDIENTE
  APROBADA
  EN_PROGRESO
  FINALIZADA
  RECHAZADA
}

enum OnboardingStatus {
  PENDIENTE
  FINALIZADO
  CANCELADO
}



enum TipoPrerrequisito {
  OBLIGATORIO
  RECOMENDADO
  CO_REQUISITO
}

enum Semestre {
  S1
  S2
  S3
  S4
  S5
  S6
  S7
  S8
  S9
  S10
}

enum EstadoMateria {
  NO_CURSADA
  EN_CURSO
  APROBADA
  APLAZADA
  RETIRADA
}

enum TipoRecurso {
  ANOTACION
  RESUMEN
  GUIA_ESTUDIO
  EJERCICIOS
  PRESENTACION
  VIDEO
  AUDIO
  DOCUMENTO
  ENLACE
  TIP
}

model Favorito {
  id          String   @id @default(cuid())
  usuarioId   String
  usuario     User     @relation(fields: [usuarioId], references: [id], onDelete: Cascade)
  recursoId   String
  recurso     Recurso  @relation(fields: [recursoId], references: [id], onDelete: Cascade)
  createdAt   DateTime @default(now())

  @@unique([usuarioId, recursoId]) // Un usuario solo puede tener un recurso como favorito una vez
  @@index([usuarioId])
  @@index([recursoId])
}

model LikeRecurso {
  id          String   @id @default(cuid())
  usuarioId   String
  usuario     User     @relation(fields: [usuarioId], references: [id], onDelete: Cascade)
  recursoId   String
  recurso     Recurso  @relation(fields: [recursoId], references: [id], onDelete: Cascade)
  createdAt   DateTime @default(now())

  @@unique([usuarioId, recursoId])
  @@index([usuarioId])
  @@index([recursoId])
}

enum TipoMeta {
  PROMEDIO_GENERAL
  MATERIAS_APROBADAS
  CREDITOS_COMPLETADOS
  SEMESTRE_ESPECIFICO
  MATERIA_ESPECIFICA
  HORAS_ESTUDIO
}

// Log de consumo de IA para estadísticas de administración
model AiUsageLog {
  id              String   @id @default(cuid())
  userId          String?
  endpoint        String   // ej. "ia/chat", "ia/resumir"
  promptTokens    Int      @default(0)
  completionTokens Int    @default(0)
  totalTokens     Int      @default(0)
  createdAt       DateTime @default(now())

  @@index([createdAt])
  @@index([endpoint])
}


/** Cupo diario de IA gratuita por usuario (modelos Gateway con precio $0). */
model IaFreeTierDaily {
  userId       String   @map("user_id")
  periodKey    String   @map("period_key")
  tokensUsed   Int      @default(0) @map("tokens_used")
  requestsUsed Int      @default(0) @map("requests_used")
  updatedAt    DateTime @updatedAt @map("updated_at")

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@id([userId, periodKey])
  @@index([periodKey])
  @@map("ia_free_tier_daily")
}

model ZenoWorkspace {
  ownerId   String   @id @map("owner_id")
  payload   Json
  updatedAt DateTime @updatedAt @map("updated_at")

  @@map("zeno_workspaces")
}

model AprendePic18Progress {
  userId    String   @id @map("user_id")
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  payload   Json     @default("{}")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@index([updatedAt])
  @@map("aprende_pic18_progress")
}

model AprendeCppPooProgress {
  userId    String   @id @map("user_id")
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  payload   Json     @default("{}")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@index([updatedAt])
  @@map("aprende_cpp_poo_progress")
}
